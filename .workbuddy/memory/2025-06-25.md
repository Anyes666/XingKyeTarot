## 2025-06-25 — 星钥塔罗 P0 Phase 1 完成

### 完成内容
- 按《星钥塔罗_P0_开发执行规格书_最终可交付版》完成 Phase 1 全部代码实现

---

## 2026-06-25 — P0.2 BugFix: 编译错误修复

### 修复轮次
- **R1**: 5类23 ERROR → 0（emotionKeywords字段/P2 API降级/flexWrap/UI闭包语句/showToast异常）
- **R2**: 3 ERROR修复（TriangleDrawPage mapScenarioToEmotionTag缺失/ShareCardPage+HistoryDetailPage UI闭包return清理）
- **R3**: TriangleDrawPage EmotionTag类型错误 → 改用HEART_OPTIONS[index].emotionTag取合法中文值

### 关键发现
- EmotionTag真实定义为中文联合类型：`'关系内耗' | '关系评估' | '职场疲惫' | '选择纠结' | '能量低落' | '寻求安慰'`
- 保存图片功能临时降级为空壳（window.screenshot/PhotoAsset.open 不兼容当前SDK）

---

## 品牌角色名替换：月见 → 星澜

- 14文件19处修改，全局零残留
- 新增常量：COMPANION_NAME='星澜', COMPANION_WATERMARK='星钥塔罗 · 星澜'
- MOON_WATERMARK 保留并标记 @deprecated，值指向新水印
- 涉及：AskHeartPage/CardDrawPage/ResultGoldenPage/FollowUpPage/EmotionStarPage/TriangleIntro/TriangleDraw/TriangleResult/RelationDraw/RelationResult + Constants/Types/Routes/TarotCard/MVP8CardsData

---

## P0.3 体验整改专项

### 整改1：保存图片真实保存
- 重写 `service/ShareImageService.ets`：componentSnapshot.createFromComponent 真实截图 → photoAccessHelper 写入相册
- 新增 SaveImageResult 接口（success/savedToAlbum/savedUri/message）
- ShareCardPage 仅 result.success && result.savedToAlbum 时 Toast "图片已保存到相册"
- 新增 isSaving 状态，按钮"保存中..."防重复点击

### 整改2：历史记录删除
- HistoryStore 新增 deleteRecordById(context, id) + clearAllRecords(context)
- HistoryPage：单条删除按钮 + 全部清空按钮，均二次确认弹窗
- HistoryDetailPage：底部"删除这条记录"按钮 + 确认弹窗
- 删除后列表自动刷新，空状态优化文案

### 整改3：牌图统一填充
- 新增 `components/TarotCardImage.ets`：ImageFit.Cover + clip(true) + 统一圆角8vp + 暗色背景
- 替换5个页面的牌图：ResultGoldenPage/TriangleResultPage/RelationResultPage/HistoryDetailPage/ShareCardPage
- 22张大阿卡纳 switch 映射 + fallback card_back

### 整改4：统一返回按钮
- 新增 `components/AppBackButton.ets`：优先 router.back()，失败走 fallbackUrl
- 覆盖13个子页面（AskHeart/ResultGolden/FollowUp/EmotionStar/PrivacyPolicy/UserAgreement/TriangleIntro/TriangleResult/RelationIntro/RelationResult/History/HistoryDetail/ShareCard）
- 不添加：LaunchPage/HomePage/CardDrawPage/TriangleDrawPage/RelationDrawPage

---

## componentSnapshot.createFromComponent 编译修复

- `ShareImageService.ets`：删除 `createFromComponent` + 全部真实保存 API（savePixelMapToGallery/packPixelMapToPng/savePngDataToGallery/ensureAlbumPermission），替换为安全降级（success=false, savedToAlbum=false）
- `DivinationResultPage.ets`：删除 `savePixelMapToGallery` 调用，Toast 从「已保存到相册」改为「保存图片功能暂未适配当前系统版本」
- 真实保存图片待单独 SDK 适配专项

---

## SaveButton + Media Library Kit 保存图片改造

- `service/ShareImageService.ets`：从降级恢复为完整保存管线（savePixelMapToGallery → packPixelMapToPng → savePngDataToGallery → photoAccessHelper.createAsset + fileIo.writeSync）
- `pages/ShareCardPage.ets`：普通 Button → 5个 SaveButton（情绪签/人设签/行动签 + 圣三角 + 关系探索）
- 截图使用 `getUIContext().getComponentSnapshot().get('shareCardContent')`（DivinationResultPage 同款 API）
- SaveButtonOnClickResult.SUCCESS 才执行保存，拒绝授权→Toast 提示权限
- 新建 6 文件 + 修改 7 文件 = 13 文件
- 链路：问心 (AskHeartPage) → 抽牌动画 (CardDrawPage) → 金句结果 (ResultGoldenPage)
- 工程师(software-engineer) 全局一致性 IS_PASS: YES
- QA(software-qa-engineer) 全部 5 大类审查 PASS，路由判定 NoOne（无反馈）

### 关键决策
- DivinationRecord 不修改，新建 MvpDivinationRecord
- Index.ets 仅插入 5 行 P0 redirect，旧代码完整保留
- Theme 双色系并存（新 Ardot + 旧新中式）
- 追问/分享/星点按钮 → Toast 占位（方案 B）
- 数据层 MVP8CardsData.ets ~686 行，8牌×6情绪 = 48 scenario 全覆盖

### 硬约束合规
1. 全部文案从 data 层读取 ✅
2. cardWeights 仅 8 张 MVP 牌 ✅
3. isReversed 固定 false ✅
4. INTERNET 权限未申请 ✅
5. v1.x 旧文件未被破坏 ✅
6. P1/P2/P3 页面未创建 ✅

## 2025-06-25 下午 — 修复 Phase 1 启动白屏 Bug

### 根因
- `main_pages.json` 未注册 `AskHeartPage`/`CardDrawPage`/`ResultGoldenPage` 三个 P0 新页面
- `Index.ets` 中的 P0 redirect 与 `EntryAbility.ets` 直接 `loadContent('pages/AskHeartPage')` 形成双重入口

### 修复
1. `main_pages.json` 追加 3 个 P0 页面注册（保留旧 6 个页面不动）
2. `Index.ets` 注释掉 aboutToAppear 中的 P0 redirect（5 行→注释），旧代码完整保留
3. EntryAbility.ets 保持不变（已正确加载 AskHeartPage）
4. 验证 card_back.png 资源存在、ComplianceFooter 存在、Theme Token 全部可用

## 2025-06-25 下午 — 修复 8 项 ArkTS 编译类型错误

### 根因
ArkTS 严格模式不允许匿名对象字面量作为类型声明，所有 `Record<string, Object>` 和 `{ key: value }` 字面量被编译器拒绝。

### 修复（4 个文件）
1. **HeartOptionsData.ets**: 新增 `CardWeightItem`/`DrawCardResult`/`EmotionWeightGroup` 接口（export）
2. **AskHeartPage.ets**: 新增 `CardDrawRouteParams`；pushUrl 参数拆为显式变量
3. **CardDrawPage.ets**: 新增 `CardDrawPageParams`/`ResultGoldenRouteParams`；路由出入参全部显式接口
4. **ResultGoldenPage.ets**: 新增 `ResultPageParams`；bracket notation → dot notation
5. 全局一致性: IS_PASS YES，无残留匿名类型

## 2025-06-25 下午 — P0.1 三页闭环打通

### 目标
把 ResultGoldenPage 三个按钮从 Toast 占位改成真实可进入页面，打通 MVP 最小可点击闭环。

### 新建 3 页面 + 修改 3 文件
| 操作 | 文件 |
|------|------|
| 新建 | `pages/FollowUpPage.ets` — 追问月见（三选一 + 固定回应 + router.back()） |
| 新建 | `pages/ShareCardPage.ets` — 分享卡（情绪签/人设签/行动签 + 保存Toast占位） |
| 新建 | `pages/EmotionStarPage.ets` — 情绪星点（7日星图静态占位 + 返回） |
| 修改 | `Routes.ets` — 追加 FOLLOW_UP/SHARE_CARD/EMOTION_STAR |
| 修改 | `main_pages.json` — 注册 3 个新页面 |
| 修改 | `ResultGoldenPage.ets` — 三按钮 Toast→router.pushUrl（具名接口+try-catch） |

### 闭环链路
```
AskHeartPage → CardDrawPage → ResultGoldenPage
                               ├→ FollowUpPage (追问月见)
                               ├→ ShareCardPage (生成分享卡)
                               └→ EmotionStarPage (情绪星点)
```
所有新页面均可通过 router.back() 返回结果页。

### 全局一致性
IS_PASS: YES — 无内联匿名对象、try-catch 包裹、路由注册完整、import 正确

## 2025-06-25 下午 — ResultGoldenPage UI 布局重组

### 目标
把主转化按钮（追问月见/分享卡）提到深度解读之前，避免被长文和滚动稀释；深度解读改为折叠区。

### 新布局
```
牌面 → 金句 → 关键词 → 今日行动 → 为什么是这张牌
→ 追问月见(主按钮) / 生成分享卡(次按钮) → 星点入口
→ 深度解读(折叠区，默认折叠) → 合规页脚
```

### 改动
- `ResultGoldenPage.ets` 单文件修改
- 新增 `isDeepInterpretationExpanded: boolean = false` State
- buildDeepInterpretation() → 折叠/展开 toggle 交互
- buildBottomEntry() → 简化为纯星点入口
- build() 区块顺序重排
- showNextPhaseToast() 保留不动

---

## 文案合规与陪伴感小修专项

### P0 必做（全部完成）
- 命运之轮去承诺：deepInterpretation「正在往上走」→「正在转动的位置，轮子往哪里转部分取决于你」
- 圣三角趋势提醒：TriangleResultPage.buildSummarySection 末尾新增固定提示文案
- 免责声明统一：Constants.COMPLIANCE_CONTENT + COMPLIANCE_FOOTER 全部改为「结果仅供娱乐与自我探索参考，不构成任何专业建议。」
- Index.ets / OnboardingPage.ets 硬编码旧文案改为常量引用

### P1 轻量做（全部完成）
- 愚人追问「今天就做第一小步」→「大决定不急着马上交出去，先给自己一个试用期」
- 战车 cardMeaningBrief「别扛了，去冲」→「先把方向看清，再让力量自然流动」
- 命运之轮追问「必须把握」→「可以把握」
- HistoryPage 副标题「最近50条占卜记录」→「回看最近的星钥记录，看看那些反复出现的心事」

### 编译
- assembleHap BUILD SUCCESSFUL，ERROR=0，WARN≈35（预先存在的 API 弃用警告）

---

## ShareCard 保存图片真实修复（最终版）

### 修改文件
- `pages/ShareCardPage.ets`：三张卡独立 id（share_card_emotion/persona/action）+ 黑金按钮 + captureCard/buildFileName/saveCard
- `service/ShareImageService.ets`：新增 savePixelMapToGalleryWithResult 返回 SaveImageResult
- `common/SafeUiActions.ets`：新建 safeToast 工具
- `common/Theme.ets`：追加 GOLD_MAIN (#D99A00) + BTN_DARK_TEXT (#0F0F23)

### 技术栈
- 截图：getUIContext().getComponentSnapshot().get(componentId, { scale: 2, waitUntilRenderFinished: true })
- 编码：image.createImagePacker + packToData (PNG, quality:100)
- 相册：photoAccessHelper.createAsset(PhotoType.IMAGE, 'png') + fileIo.openSync + writeSync + closeSync
- 权限：WRITE_IMAGEVIDEO 已存在，无需新增

### 关键判断
- success && savedToAlbum 双重判断才 Toast "图片已保存到相册"
- 否则 Toast result.message
- 截图失败 → "截图失败，请稍后再试"
- 异常 → "保存失败，请稍后再试"

### 编译
- BUILD SUCCESSFUL, ERROR=0, WARN=80（全项目，8条与本次相关但均为原有 deprecated API）

---

## ShareCard 保存分阶段诊断（待真机日志）

### 诊断字段
SaveImageResult 扩展：stage / errorCode / errorMessage

### 每步独立 try/catch + 日志
- pack: `[ShareImage] pack success, length=...` / `failed at pack, code=..., message=...`
- writeAsset（临时文件）: `write temp file success, path=...` / `failed at writeAsset`
- createAsset: `showAssetsCreationDialog start, srcUri=...` / `createAsset success, uri=...` / `failed at createAsset`
- cancel: 用户取消 showAssetsCreationDialog → stage='cancel'
- cleanup: 临时文件清理

### 关键切换
- 从 `photoAccessHelper.createAsset(PhotoType.IMAGE, 'png')` 切换为 `showAssetsCreationDialog(srcUris, configs)` + PhotoCreationConfig
- 先写 cacheDir 临时文件 → 再 dialog 授权 → 系统复制到相册
- module.json5 已有 WRITE_IMAGEVIDEO 权限 + save_image_reason

### ShareCardPage 诊断日志
- `[ShareCard] capture start, componentId=...` / `capture success` / `capture failed`
- `[ShareCardPage] save failed stage=..., code=..., message=...`

### 状态
- 代码已修复，assembleHap 通过
- 待真机/模拟器运行后从日志读取实际失败 stage
