# 2026-06-26 工作日志

## EmotionStarPage 底部按钮 BugFix（软件团队 SOP）

### 问题
EmotionStarPage 底部「← 回到结果页」按钮点击无响应。根因：Button 完全没绑定 `.onClick`。

### 修复
- 文件：`entry/src/main/ets/pages/EmotionStarPage.ets`（唯一改动文件）
- 新增 `private backToResultPage(): void`：`router.back()` + try/catch，失败 fallback `router.replaceUrl({url: Routes.HOME})`。逻辑与左上角 `AppBackButton.handleBack` 等价。
- 给底部 Button 补 `.onClick(() => { this.backToResultPage(); })`，样式属性全部保留。

### 教训（重要）
1. 工程师寇豆码初版自作主张加了 `if (router.getLength() <= 1)` 前置检查，但当前 SDK 里 `router.getLength()` 返回 **string**，不能和 number 比较 → 编译 ERROR。工程师报告"成功"但实际未真实跑通编译，违反"绝不假成功"铁律。
2. QA 严过关静态审查未发现该类型错误（因为没跑编译）。**主理人亲自跑 assembleHap 才抓到**。
3. 修复方式：移除 `getLength()` 检查，让方法与 AppBackButton 完全等价（直接 back + catch fallback）。

### 编译结果
- `hvigorw.bat assembleHap` → **BUILD SUCCESSFUL**
- ERROR = 0
- WARN：EmotionStarPage 仅 3 个 deprecation（getParams/back/replaceUrl，与全项目既有模式一致，不在本轮范围）
- 编译命令路径：`D:/HarmonyOS/DevEco Studio/tools/hvigor/bin/hvigorw.bat`（项目根目录无 hvigorw，需用 DevEco 自带的）

### 验收
- 底部按钮是真 Button + 有 onClick + 调 backToResultPage
- 左上角 AppBackButton 未动
- 未改保存图片/路由参数/其他页面/main_pages.json/module.json5
- fallback 到 Routes.HOME('pages/Index')，合法已注册页面，不白屏

### 团队
- team: software-bugfix-emotion-star-back（已删除）
- 工程师修复 + QA 独立审查 + 主理人亲跑编译验证 = 三层把关，第三层抓到前两层漏掉的真实 ERROR

## 新用户三页 Onboarding 接入专项（软件团队快速模式）

### 任务
新用户同意协议后先进入 3 页 Onboarding，再进 AskHeartPage(FIRST_EXPERIENCE)。

### 改动文件（只有 3 个）
1. `entry/src/main/ets/common/AppStateStore.ets` — 新增 KEY_ONBOARDING_COMPLETED/KEY_ONBOARDING_COMPLETED_AT 常量 + hasCompletedOnboarding/completeOnboarding 方法（风格与 isFirstExperienceCompleted 一致）
2. `entry/src/main/ets/pages/LaunchPage.ets` — routeAfterPrivacy 改 4 段判断：onboarding(false→ONBOARDING) → firstExperience(false→AskHeart FIRST_EXPERIENCE) → NEW_HOME。onAccept 未改（调 routeAfterPrivacy 自动路由到 ONBOARDING）
3. `entry/src/main/ets/pages/OnboardingPage.ets` — 全量重写：移除 Swiper/AppBackButton/跳过/旧图/StarBackground/PrimaryButton/UserStateService/display.px2vp；三页 if 分支切换，currentIndex 0/1/2；无返回/无跳过；第3页调 AppStateStore.completeOnboarding + replaceUrl AskHeart FIRST_EXPERIENCE

### 关键决策
- onboarding 判断必须在 firstExperience 之前（铁律，已遵守）
- 图片资源名：onboarding_self_seeing / onboarding_daily_star_key / onboarding_feature（**单数 feature**）
- UserStateService.completeOnboarding 只写旧 key(is_onboarding_completed)，新 OnboardingPage 改用 AppStateStore 新 key(onboardingCompleted)，互不干扰（只读核查，未改 UserStateService）
- Constants.COMPLIANCE_FOOTER 文案与 spec 一致，第1页免责声明直接复用
- getUIContext().getHostContext() as common.UIAbilityContext 是现有标准模式，保留

### 复核流程
- 工程师报告 BUILD SUCCESSFUL ERROR=0
- 主理人逐行读 3 个文件核对：4段判断顺序/三页文案/图片资源名/无返回跳过/ArkTS规则全通过
- 主理人亲跑 assembleHap：BUILD SUCCESSFUL（3.9s缓存），ERROR=0，grep无ERROR行
- 三层把关全通过，未走独立QA（快速模式+编译已验证+代码已逐行核对）

### 编译结果
- BUILD SUCCESSFUL
- ERROR = 0
- WARN = 77（基线一致，OnboardingPage新增1个replaceUrl deprecated警告，与全项目同模式，spec要求保留）

### Routes/main_pages.json 状态
- Routes.ets 已有 ONBOARDING='pages/OnboardingPage'（无需改）
- main_pages.json 已注册 pages/OnboardingPage（无需改）

### 待用户实机验证
- 清应用数据/卸载重装测首次启动：协议→Onboarding三页→AskHeart FIRST_EXPERIENCE
- 测二次启动分流四种情况

## AskHeartPage 首次体验跳过入口专项（工程师 abort，主理人接力完成）

### 任务
AskHeartPage 在 FIRST_EXPERIENCE 模式下增加「暂时跳过，先逛首页」入口，点击后调 completeFirstExperience + replaceUrl NEW_HOME。

### 过程教训（重要）
- 工程师 agent 跑了 9 分 17 秒后 **abort 失败**（可能超时或被中断）
- abort 前工程师已把改动写完（跳过入口 if 分支 + skipFirstExperience 方法）
- 主理人此前已先加了 import AppStateStore，工程师又加一次 → **import 重复**（line 7-8），会致 Duplicate identifier ERROR
- 主理人接力：重读文件发现重复 import，删一行，跑编译 BUILD SUCCESSFUL
- **教训**：agent abort 后不要假设没改动，必须重读文件看实际状态；多次 Edit 同一文件要检查 import 重复

### 改动文件（只有 1 个）
`entry/src/main/ets/pages/AskHeartPage.ets`：
- 新增 import AppStateStore
- 主按钮下方、ComplianceFooter 上方新增 if(FIRST_EXPERIENCE) 跳过入口（方案A文字按钮，透明背景，#94A3B8 字色，真 Button）
- 新增 skipFirstExperience 方法：completeFirstExperience → replaceUrl Routes.NEW_HOME，失败 toast 不跳转（不假成功）

### 关键决策
- 跳转目标用 Routes.NEW_HOME（'pages/HomePage'）不是 Routes.HOME（'pages/Index'）—— 与 LaunchPage 启动链路一致
- 复用 AppStateStore.completeFirstExperience（不改 AppStateStore）
- 写入失败时 toast 提示并 return，用户仍可继续抽牌（不阻断）

### 编译结果
- BUILD SUCCESSFUL（3.7s 缓存）
- ERROR = 0
- WARN = 77（基线一致，AskHeartPage 无新增 WARN）

### 未碰文件
AppStateStore/LaunchPage/OnboardingPage/Routes/main_pages.json/ShareCardPage/ShareImageService/EmotionStarPage/FollowUpPage/保存图片逻辑 全未碰

### 待用户实机验证
- 首次体验跳过：清数据→协议→Onboarding→AskHeart→点跳过→HomePage→重启不回 AskHeart
- 普通入口不显示跳过：从 HomePage 进单牌快占，确认无跳过入口

## HistoryPage 空状态按钮 + ShareCardPage 单牌卡牌面（两阶段修复）

### 阶段1：HistoryPage 空状态按钮
- 文件：`entry/src/main/ets/pages/HistoryPage.ets`
- 原问题：buildEmptyState 按钮写死「去单牌快占」+ 跳 CARD_DRAW，4种筛选都不变
- 修复：新增 getEmptyActionText() + goEmptyAction() 两个 helper
  - 圣三角→「去圣三角」→ Routes.TRIANGLE_INTRO
  - 关系→「去关系探索」→ Routes.RELATION_INTRO
  - 全部/单牌→「去单牌快占」→ Routes.ASK_HEART（不再跳 CARD_DRAW，统一改 ASK_HEART）
- 按钮样式属性全部保留，只改文案绑定和 onClick

### 阶段2：ShareCardPage 单牌卡牌面
- 文件：`entry/src/main/ets/pages/ShareCardPage.ets`
- 原问题：buildSingleLayout 三张签海报只有文字，缺卡牌面
- 修复：新增 @Builder buildSingleCardFace()，复用已有 TarotCardImage 组件（真实卡牌图）
  - cardId 取 record.cards[0].cardId，cardWidth 96 cardHeight 150
  - 外层 Column 宽120 黑金描边（#0F0F23 背景 + #8B6A2B 描边 + borderRadius 16）
  - 下方牌名（GOLD_LIGHT）+ 副标题「此刻照见你的星钥」（MOON_MUTED）
  - 插入三张签海报的「✦ XX签」标题之后、主文案之前
- 关键边界：卡牌面在 .id 容器内（会被截图），buildSaveButton 位置不动（在海报外）

### 编译结果
- 阶段1 BUILD SUCCESSFUL（17.4s）ERROR=0
- 阶段2 BUILD SUCCESSFUL（17.2s）ERROR=0
- 主理人复核：逐行核对 getEmptyActionText/goEmptyAction 逻辑 + buildSingleCardFace 三处调用位置 + 保存按钮边界
- 主理人亲跑 assembleHap：BUILD SUCCESSFUL（4.1s缓存）ERROR=0
- WARN=77 基线一致，零新增

### 未碰文件
ShareImageService/EmotionStarPage/FollowUpPage/OnboardingPage/LaunchPage/HomePage/AskHeartPage/CardDrawPage/ResultGoldenPage/TriangleResultPage/RelationResultPage/Routes/main_pages.json/module.json5/牌库/TarotCardImage组件/HistoryStore 全未碰

### 待用户实机验证
- HistoryPage 4种筛选空状态按钮文案+跳转
- ShareCardPage 单牌三张签海报显示卡牌面
- 保存图片仍正常（保存按钮在海报外，截图包含卡牌面）
