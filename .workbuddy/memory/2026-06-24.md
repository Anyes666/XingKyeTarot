# 2026-06-24

## OnboardingPage 图片优化
- 为新手引导3页分别添加了对应图片：权杖一(抽一张牌)、权杖三(圣三角牌阵)、太阳(每日一占)
- 图片源：2张来自AI生成塔罗牌图、1张来自已有大阿卡纳
- 多次调整：去边框→去黑角(Stack裁剪方案)→调间距→定版

## DivinationHistoryPage 修复
- 修复「清除」按钮文字被挤成省略号的问题：去掉 `width(56)` 改用 `padding` 自适应

## LegalDocuments 版本号
- 添加 `APP_VERSION = 'v1.1.0'`，自动注入用户协议和隐私政策

## Roadmap 对照
- 逐条对比 XingKey_Tarot_Roadmap_v1.1_to_v2.0.docx，产出完整缺陷清单
- 写入 `docs/release/defects-and-reminders.md`

## 多分辨率适配 + 平滑加载（Phase 3）
- Theme.ets: 新增 TAROT_CARD_ASPECT_RATIO + 响应式断点常量
- TarotCardView.ets: 重写为 @Builder 模式，加入：
  - 图片加载骨架屏（LoadingProgress）
  - 加载失败兜底（"?" + 文案）
  - .onComplete() → 淡入动画（opacity 0→1, 300ms EaseOut）
  - .onError() → 降级显示错误状态
  - @StorageProp('windowWidth') 响应式断点：手机/平板不同尺寸
- OnboardingPage.ets: 图片尺寸响应式 + 加载/错误状态
- Index.ets: aboutToAppear 中通过 display.getDefaultDisplaySync() 初始化 windowWidth 到 AppStorage
- OnboardingPage.ets: 同样初始化 windowWidth（新用户先走引导页）

## 首页塔罗库占位入口（v1.1 收尾）
- Index.ets: 新增 TarotLibraryEntry() @Builder，在 HistoryEntry 下方
- 样式：暗色半透明背景 + 细白边框，"塔罗库" + "78张牌全览·即将上线" + "敬请期待"
- 点击弹出 Toast "塔罗库即将上线，敬请期待"
- defects-and-reminders.md: P0-1 标记为已修复 ✅
- **v1.1 Phase 0 只剩 P0-2(截屏) 和 P0-3(Release构建验证)**
