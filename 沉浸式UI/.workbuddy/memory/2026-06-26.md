# 2026-06-26 工作日志

## 沉浸式示例项目扫描（只读）

- 任务：扫描 `D:\XingKeyTarot\沉浸式UI`（华为官方沉浸式示例），输出分析报告供星钥塔罗参考
- 严格只读：未修改示例项目、未修改星钥塔罗、未执行 assembleHap
- 扫描发现：
  - 示例项目 SDK 版本：`6.0.0(20)`，EntryAbility 极简，未调用 `setWindowLayoutFullScreen`，未设系统栏颜色
  - 演示 3 种方案：方案 A（`background()` 背景沉浸）、方案 B+E（`ignoreLayoutSafeArea` + 动态避让）、方案 D 变体（自由窗口标题栏沉浸）
  - **未使用 `expandSafeArea`**（全项目零命中）
  - 核心工具类：`commons/commons/src/main/ets/utils/WindowUtil.ets`（含 getWindowAvoidArea / on('avoidAreaChange') / px2vp 转换）
- 对星钥塔罗的建议：**方案 A 首选**（页面根容器 `.background('#0F0F23')`），改动最小、零 Window API、Scroll 无副作用；特殊页（启动页全屏图）才用方案 B+E，padding 必须 px2vp 转换
- 报告落盘：`D:\XingKeyTarot\沉浸式UI\沉浸式示例项目扫描报告.md`
- 下一轮若要改星钥塔罗：先核对 `build-profile.json5` 的 `compatibleSdkVersion`，再从一个白边最明显的页面验证方案 A
